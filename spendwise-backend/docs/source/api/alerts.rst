``alerts`` service
==================

Listing, alert status checks, and dismissal of alerts attached to the
current user. Alerts are produced when an expense exceeds the budget
configured for its category, and the new ``/statuses`` endpoint exposes
real-time comparisons between actual spending and user-defined monthly
limits.

.. automodule:: services.alerts.routes
   :members:
