

RUN BACKEND
```
cd spendwise-backend
```
```
uvicorn app.main:app --reload
```

localhost:8080
localhost:8080/docs

RUN FRONTEND:
```
cd spendwise-front
```

if necessary:
    ``npm install`` or ``npm i``
    run project:
```
npm run dev
```
